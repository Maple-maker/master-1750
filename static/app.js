/* app.js — intentionally empty.
 *
 * All front-end logic is inlined in templates/index.html for simplicity
 * (no build step, single source to edit). This file exists so the static/
 * directory is non-empty and available for future extraction of the JS.
 */
